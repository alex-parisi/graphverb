#ifndef GRAPH_NODE_H
#define GRAPH_NODE_H

/**
 * @brief Structure to represent a node in the spectral graph.
 */
struct GraphNode {
    int index;
    float frequency;
    float magnitude;
};

#endif // GRAPH_NODE_H
